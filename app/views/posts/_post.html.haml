.row.post.big-post-frame
  .col-xs-8
    = link_to post, target: '_blank' do
      = cl_image_tag post.cloudinary_id, width: 397

  .col-xs-4
    / likes
    = render 'partials/like_button', post: post

    / post info
    %h4
      = link_to post, class: 'black-link' do
        = truncate post.title.titleize, length: 20

    %p
      By
      = link_to post.user.full_name, user_path(post.user)

    %p
      = time_ago_in_words post.created_at
      ago

    %br

    - if post.description.present?
      = simple_format truncate(post.description, length: 250)
