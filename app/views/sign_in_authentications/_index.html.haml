%p
  - if fb_auth = current_user.sign_in_authentications.find_by(provider: 'facebook')
    = link_to fb_auth,
              method: :delete,
              remote: true,
              class: 'btn btn-facebook btn-block',
              title: 'Click to remove' do
      Sign in with Facebook
  - else
    = link_to user_omniauth_authorize_path(:facebook),
              class: 'btn btn-default btn-block',
              title: 'Click to add' do
      Sign in with Facebook
%p
  - if gg_auth = current_user.sign_in_authentications.find_by(provider: 'google')
    = link_to gg_auth,
              method: :delete,
              remote: true,
              class: 'btn btn-google btn-block',
              title: 'Click to remove' do
      Sign in with Google
      &nbsp;&nbsp;&nbsp;
  - else
    = link_to user_omniauth_authorize_path(:google),
              class: 'btn btn-default btn-block',
              title: 'Click to add' do
      Sign in with Google
      &nbsp;&nbsp;&nbsp;
